#!/usr/bin/env sh

echo "▶ Pre-commit: type checking + build (astro check && astro build)..."
pnpm build || exit 1

echo "▶ Pre-commit: running unit tests (Vitest)..."
pnpm test:unit || exit 1

echo "▶ Pre-commit: running E2E tests (Playwright)..."
pnpm test:e2e || exit 1

echo "✓ Pre-commit checks passed."
